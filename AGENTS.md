# Claude Code 指示ファイル — 3D-bunkasai-2026

## ⚠️ 絶対ルール：作業したら必ずpushまで完了させること

作業の区切り（ファイル作成・修正・方針の決定など）のたびに、
**その場で必ず `git add → commit → push` を完了させること。**

- 「後でまとめて」「別の環境から改めてpush」は禁止。作業したその場でpushする。
- ローカル（自宅PC）でもクラウド（Claude Code on the web）でも、pushしていない変更は他の環境から一切見えない。
  （2026-07-14〜19：ローカル（VS Code）で作成した映像再生ツールの作業が5日間pushされずに残っていた一方、
  同じ期間にクラウド側では並行して別の作業が10件以上進み、mainが大きく先行していた実例あり。
  pushが遅れるほど後から一方がもう一方に追いつく際のコンフリクト解消コストが増える。同じ失敗を繰り返さないこと）
- ファイルを1つでも作成・編集したら、それだけでpushの対象。
- 会話の中だけで決めた方針・決定事項（ファイルへの反映がまだのもの）も、
  そのまま会話を終えず `生徒用/変更点.md` や関連ファイルに書き起こしてからpushする。
  会話ログ自体はGitに残せないため、「何を決めたか」を必ずファイルの形で残すこと。

## 必須：作業開始前に必ず読むこと

作業を始める前に、必ず以下を **この順番で** 読むこと：

```
1. プロジェクト概要.md   ← 基本情報・フォルダ構成・重要ルール・技術仕様・次の課題
2. 生徒用/変更点.md      ← 直近の変更履歴（上の3〜5エントリを確認）
```

`プロジェクト概要.md` の「現在の状態・引継ぎ情報」セクションに技術仕様や演出上の決定事項が記載されている。  
`変更点.md` で直近に何が変わったかを把握してから作業すること。読まずに作業を始めてはならない。

## Claudeの動き方（2026-07-16追加・同日改訂：ユーザーからの明示的な方針）

- ユーザーはClaude（このセッション）を「社長」、Agentツールで呼び出すサブエージェントを「社員（agents）」と呼んでいる。
- **（2026-07-16改訂・最終方針）作業は基本的に全部agentsにやらせること。** 調査・下調べだけでなく、実際のファイル編集・整合性確認・PDF再生成・git操作（コミット・push・PR作成）まで含めて、実務はagentsに担当させる。
- **例外：PRのマージだけはClaude本体が最終確認してから行うこと**（2026-07-16追加）。理由：agentがPRを作成しその場で自分でマージすると、セキュリティチェックで「レビューなしの自己マージ」と警告が出る（agent自身の会話には、このセッションでユーザーが既に許可した経緯が見えないため）。実害は無いことが多いが、ユーザーの意向で、マージの最終判断だけはClaude本体を経由する運用にした。agentsにはコミット・push・PR作成までを担当させ、マージはClaude本体が内容を確認したうえで実行する。
- Claude本体（社長）の役割：
  - ユーザーと会話し、何をやりたいか・何が大事な決定事項かを引き出す
  - その内容をagentsに過不足なく伝える（agentsは今の会話を見ていないので、必要な背景・過去の決定事項・ファイルパスなどを指示に含めること）
  - agentsからの報告を受け取り、ユーザーに分かりやすく伝える
  - ユーザー自身の判断が要る場面（配役・演出・予算など実世界の決定）はAskUserQuestion等でユーザーに確認する
  - つまり社長自身はほとんど手を動かさない「ずるい社長」でよいが、**ユーザーとのやり取りで得た重要な情報・決定事項は、agentsへの指示に漏れなく含めること**（agentsに丸投げしても、必要な文脈を渡し忘れて質の低い作業にならないよう注意する）
- 実務上の注意（agentsに任せる際の分け方）：
  - 複数ファイルにまたがる整合性が必要な変更や、同じファイルへの複数箇所編集は、**同じagent（1体）にまとめて任せる**か、範囲を明確に分担させて競合を避けること（複数agentsが同じファイルに同時書き込みすると変更が失われることがある）
  - git操作のうちコミット・push・PR作成はagentsに担当させてよいが、**マージはClaude本体が行う**（上記「例外」参照）
- 理由：LLMは会話コンテキストが大きく・雑多になるほど注意が薄まり精度が落ちる（いわゆるcontext rot）。単純に「頭を使うと疲れる／バカになる」という話ではなく、**コンテキストの肥大化による精度低下を避ける**ことが目的。この方針は本セッションだけでなく、今後別スレッド・別セッションのClaudeも踏襲すること。

## 作業中に守ること

- ファイルを上書き・削除する前に `過去版/` へコピーしてアーカイブする（命名：`元ファイル名_YYYYMMDD.拡張子`）
- 作業後は `生徒用/変更点.md` に時刻付きで記録する
- **時刻を記録する前に、必ず日本時間（JST）で現在時刻を取得すること**（2026-07-16追加）。Linux環境（Claude Code on the web等）はシステムクロックがUTCのため、`date`コマンドをそのまま使うと日本時間より9時間遅れた時刻になる。`TZ=Asia/Tokyo date +"%Y-%m-%d %H:%M"`のようにタイムゾーンを明示して取得すること（Windows環境では`Get-Date -Format "yyyy-MM-dd HH:mm"`でよい）
- ユーザーから新しいルール・方針・決定事項を伝えられたら `プロジェクト概要.md` の重要ルールに追記する
- 上記の変更はすべてコミット・GitHubへプッシュする
